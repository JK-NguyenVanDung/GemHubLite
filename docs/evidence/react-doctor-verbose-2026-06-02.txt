React Doctor v0.2.15

  

  Bugs › 10 warnings
  Maintainability › 19 warnings
  Performance › 5 warnings

  34 issues

  ⚠ react-doctor/no-inline-exhaustive-style        ×10
      This inline style has 8 properties, which is hard to read & rebuilds every render. Move it to a CSS class, CSS module, or styled component.
      → Move the styles to a CSS class, CSS module, Tailwind utilities, or a styled component. Big inline objects are hard to read and rebuild on every update.
      Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://www.react.doctor/docs/rules/react-doctor/no-inline-exhaustive-style
      src/features/product-detail/components/ProductHero.tsx:21
      src/components/ui/Thumbnail/Thumbnail.tsx:38
      app/(tabs)/_layout.tsx:17
      src/features/products/components/ProductCard.tsx:44
      src/features/camera/components/CaptureReview.tsx:627
      src/features/camera/components/CaptureReview.tsx:906
      src/features/camera/components/CaptureReview.tsx:943
      src/features/camera/components/CaptureReview.tsx:962
      src/features/camera/components/CaptureReview.tsx:1110
      src/features/camera/components/CaptureReview.tsx:1275

  ⚠ react-doctor/rn-no-scrollview-mapped-list
      Your users get slow scrolling when <ScrollView> with items.map(...) builds every row at once.
      → `<ScrollView>{items.map(...)}</ScrollView>` builds every row at once, which slows scrolling. Use FlashList, LegendList, or FlatList instead.
      Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://www.react.doctor/docs/rules/react-doctor/rn-no-scrollview-mapped-list
      src/features/product-detail/components/MediaStrip.tsx:36

  ⚠ react-doctor/prefer-useReducer                 ×4
      7 useState calls in "StableCameraPreview" can each trigger a separate render.
      → Group related state: `const [state, dispatch] = useReducer(reducer, { field1, field2, ... })`
      Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://www.react.doctor/docs/rules/react-doctor/prefer-useReducer
      src/features/camera/components/CameraView.tsx:112
      app/(tabs)/media.tsx:26
      src/features/camera/components/CaptureReview.tsx:87
      app/(tabs)/products.tsx:29

  ⚠ react-doctor/async-await-in-loop
      This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
      → Collect the items, then use `await Promise.all(items.map(...))` so independent work runs at the same time
      Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://www.react.doctor/docs/rules/react-doctor/async-await-in-loop
      src/features/camera/hooks/useCaptureSave.ts:36

  ⚠ react-doctor/async-defer-await
      This await blocks the function before an early-return that doesn't use the awaited value, so the skip path waits for nothing. Move the await below the guard so it only runs when you need the data
      → Move the `await` below the early-return guard so the skip path stays fast
      Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://www.react.doctor/docs/rules/react-doctor/async-defer-await
      src/lib/db/repositories/captureSave.ts:25

  ⚠ react-doctor/rerender-memo-before-early-return ×2
      This runs even when the component bails out because the useMemo builds JSX before an early return, so move the JSX into a child wrapped in memo to skip it on the early return
      → Move the JSX into a child component wrapped in memo, so the parent's early return skips it
      Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://www.react.doctor/docs/rules/react-doctor/rerender-memo-before-early-return
      app/(tabs)/media.tsx:128
      app/(tabs)/products.tsx:120

  ⚠ react-doctor/rn-scrollview-dynamic-padding     ×4
      Your users see rows jump when a changing paddingBottom on contentContainerStyle shifts the whole list.
      → Use `contentInset={{ bottom: dynamicValue }}` so the OS shifts the content instead of relaying it out, which avoids the jump.
      Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://www.react.doctor/docs/rules/react-doctor/rn-scrollview-dynamic-padding
      app/(tabs)/media.tsx:165
      src/features/camera/components/CaptureReview.tsx:785
      src/features/camera/components/CaptureReview.tsx:1103
      app/(tabs)/products.tsx:157

  ⚠ react-doctor/no-react19-deprecated-apis
      useContext is replaced by `use()` in React 19, which reads context inside ifs & loops too, so switch to `import { use } from 'react'`.
      → Pass `ref` as a normal prop on function components, since `forwardRef` isn't needed in React 19. Replace `useContext(X)` with `use(X)`. Only runs on React 19+ projects.
      Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://www.react.doctor/docs/rules/react-doctor/no-react19-deprecated-apis
      src/theme/useTheme.ts:1

  ⚠ react-doctor/rn-prefer-reanimated
      Your users see stutter when Animated from react-native runs on the JS thread.
      → Use `import Animated from 'react-native-reanimated'` so animations run on the UI thread instead of the JS thread, which keeps them smooth.
      Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://www.react.doctor/docs/rules/react-doctor/rn-prefer-reanimated
      app/camera.tsx:3

  ⚠ react-doctor/no-giant-component
      Component "CaptureReview" is 505 lines long, which is hard to read & change. Split it into a few smaller components.
      → Pull each section into its own component, like `<UserHeader />` and `<UserActions />`.
      Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://www.react.doctor/docs/rules/react-doctor/no-giant-component
      src/features/camera/components/CaptureReview.tsx:87

  ⚠ react-doctor/rerender-state-only-in-handlers
      Each update to "detectedCode" redraws your component for nothing because this useState is set but never shown on screen.
      → Use useRef instead of useState when the value is only set and never shown on screen. `ref.current = ...` updates it without redrawing the component.
      Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://www.react.doctor/docs/rules/react-doctor/rerender-state-only-in-handlers
      src/features/camera/components/CaptureReview.tsx:991

  ⚠ deslop/unused-file
      Unused file — not reachable from any entry point
      → Delete the file if it is truly unreachable, or import it from an entry point.
      src/lib/files/mediaFiles.ts

  ⚠ deslop/unused-export                           ×6
      Unused export: `ProductNotFoundError`
      → Drop the `export` keyword (or remove the declaration) if no other module uses this symbol.
      src/lib/db/repositories/media.ts:27
      src/lib/files/media-storage.ts:38
      src/lib/files/media-storage.ts:39
      src/lib/files/media-storage.ts:40
      src/lib/files/media-storage.ts:52
      src/lib/files/media-storage.ts:68

Agent guidance
  - Treat React Doctor diagnostics as starting hypotheses. Read the relevant code before confirming or suppressing each finding.
  - For each group, decide true positive, false positive, or needs-human-review, then assign high/medium/low confidence.
  - Do not suppress a finding without evidence from the file in question. Confidence requires code context.
  - Understand the root cause before editing. Fix the underlying code instead of changing react-doctor config or suppressing rules unless explicitly asked.
  - Investigate deeply where relevant: race conditions, security-sensitive flows, state propagation, multi-file refactors, and downstream dependency chains.
  - Ignore pure style preferences, theoretical issues without real impact, missing features, and unrelated pre-existing code.
  - Start with high-confidence fixes that preserve behavior. Leave low-confidence or product-dependent changes as notes.
  - Run `npx react-doctor@latest --verbose --diff` before and after changes, plus relevant tests after each focused batch.
  - When available, spawn subagents or isolated worktrees for independent rule families, then review and merge only the best safe fixes.
  - Split unrelated, broad, or behavior-changing work into separate PRs/branches instead of one large cleanup.
  - For confirmed issues that cannot be fixed now, create GitHub issues with the rule, file/line, confidence, impact, and proposed fix.
  - If a fix needs an API, UX, or architecture decision, stop and ask before editing.

  React Doctor (https://react.doctor)
  Score disabled by --no-score. Want something custom to your company? Contact us at https://react.doctor/enterprise.

  Full diagnostics written to /var/folders/mn/7162t97n5wq4wp8qksrl_chw0000gp/T/react-doctor-9c31a4f6-c1e0-451d-af63-bd59f77be2e8

  Docs: https://www.react.doctor/docs
  Set up CI/CD, suppress rules with a config file, and scan diffs or PRs.


React Doctor is not installed in this project.
Ask the user if they would like to set it up, then run:

  npx react-doctor install --yes

This adds agent skill files, a `doctor` package script, and react-doctor as a dev dependency.
